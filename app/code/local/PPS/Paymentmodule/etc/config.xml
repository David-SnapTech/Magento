<?xml version="1.0"?>
<config>
  <modules>
    <PPS_Paymentmodule>
      <version>0.1.0</version>
    </PPS_Paymentmodule>
  </modules>

  <global>
    <!-- IMPORTANT: if you use your own namespace (i.e. CompanyName) you also have to declare blocks group for new module. See topic: http://www.magentocommerce.com/boards/viewthread/22416/#t102732 -->
    <blocks>
      <paymentmodule>
        <class>PPS_Paymentmodule_Block</class>
      </paymentmodule>
    </blocks>

    <!-- declare model group for new module -->
    <models>
      <!-- model group alias to be used in Mage::getModel('newmodule/...') -->
		<paymentmodule>
			<!--class>CompanyName_NewModule_Adminhtml</class-->
			<class>PPS_Paymentmodule_Model</class>
		</paymentmodule>
    </models>

    <!-- declare resource setup for new module -->
    <resources>
      <!-- resource identifier -->
      <paymentmodule_setup>
        <!-- specify that this resource is a setup resource and used for upgrades -->
        <setup>
          <!-- which module to look for install/upgrade files in -->
          <module>PPS_Paymentmodule</module>
        </setup>
        <!-- specify database connection for this resource -->
        <connection>
          <!-- do not create new connection, use predefined core setup connection -->
          <use>core_setup</use>
        </connection>
      </paymentmodule_setup>
      <paymentmodule_write>
        <connection>
          <use>core_write</use>
        </connection>
      </paymentmodule_write>
      <paymentmodule_read>
        <connection>
          <use>core_read</use>
        </connection>
      </paymentmodule_read>
    </resources>
  </global>

  <!-- declare default configuration values for this module -->
  <default>
    <!-- 'payment' configuration section (tab) -->
    <payment>
      <!-- 'newmodule' configuration group (fieldset) -->
      <paymentmodule>
        <!-- by default this payment method is inactive -->
        <active>0</active>
        <!-- model to handle logic for this payment method -->
        <model>paymentmodule/paymentMethod</model>
        <!-- order status for new orders paid by this payment method -->
        <order_status>pending</order_status>
        <!-- default title for payment checkout page and order view page -->
        <title>Credit Card (Priority Payment Systems)</title>
        <cctypes>AE,VI,MC,DI</cctypes>
        <payment_action>authorize</payment_action>
        <allowspecific>0</allowspecific>
      </paymentmodule>
    </payment>
  </default>
</config>